#include <bits/stdc++.h>
using namespace std;
// This file include all standard library.
// by using this we don't have to include standard library header file
#include <iostream>
using namespace std;
// to use input/output services for c++
// like cin, cout, cerr and clog
#include <iomanip>
// this contains the function that we can use to format the output for c/c++
// These functions can be used one at a time or together to make the output of our program more presentable.
// These functions can be very useful when we need to format and beautify the output.
#include <string>
// we use this when we use a variable that has type std::string for c++
// by using this we can include string variable in the code
#include <cstring>
#include <fstream>
// used to control the data to read from a file as an input and data to wirte into the file as an output for c++
#include <ctime>
#define PI 3.14
// "h" is the header in the C standard library for the C programming language which contains macro definitions
#include <stdio.h>
// to use input/output function for c/c++
// liek printf(), scanf()
// "h" extension means header file.
// to define some value for c/c++
#include <conio.h>
// console input/output header file for c/c++
#include <math.h>
// to use math function for c/c++
#include <string.h>
// to use string function for c/c++
// like strlen(), strcpy(), strcat(), strcmp(), strrev(), strlwr() and strupr()
#include <ctype.h>
// this header file provide character manupulation rlated library function for c/c++
// like isalpha(), isdigit(), toupper(), tolower(), isprint()
#include <time.h>
// this perform functions related to date and time for c/c++
// like setdate() and getdate().
// to modify the system date and get the cpu time respectively.
#include <conio.h>
// this have function call getch() which Take a keyboard input
#include <malloc.h>
#include <vector>
#include <list>
#include <map>
#include <functional>
#include <algorithm>
// to include STL
#include <iomanip>
// The header <iomanip> is part of the Input/output library of the C++ Standard Library. It defines the manipulator functions resetiosflags(), setiosflags(), setbase(), setfill(), setprecision(), and setw(). These functions may be conveniently used by C++ programs to affect the state of iostream objects
int main()
{
    return 0;
}
